# Examples

コア FW はスタック非依存。ここには参考実装を 1 本置く。

- **default:** [taskboard](./taskboard/) — React / Hono / Vitest / Playwright / Prisma(schema) / Terraform 最小

`/bootstrap-product` は上記 default を `product/` へ展開し、本ディレクトリを削除する。
