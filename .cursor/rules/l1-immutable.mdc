---
description: L1 憲法ディレクトリの編集制約
globs: specs/L1/**
alwaysApply: false
---

# L1 編集制約

このパスは Agent が直接編集しない。ユーザーが Steward を明示し承認した場合のみ変更する。変更時は `VERSION` と各ファイルの version を揃える。ADR／chore PBI は必須ではない — 判断が重いとき Agent が推奨し、作成はユーザー確認後（`CONTEXT.md` の ADR）。
