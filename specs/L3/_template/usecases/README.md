---
layer: L3
domain: _template
kind: usecases
maturity: draft
actors: []
---

# ドメインユースケース（テンプレ）

`specs/L3/<app-domain>/` にコピーして使う。`<app-domain>` はアプリ関心ドメイン名（技術領域名ではない）。例: `tasks`。

個別ユースケースは `agents/pipeline/promote/templates/usecase.md` をコピーし、`actors` に1つ以上のアクターを書く（単一・複数可）。
