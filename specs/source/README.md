# specs/source

機能 PRD（Source）の置き場。**Promote 前の仮仕様**。`/spec-source` が書き、`/promote` が L2/L3 へ取り込んだ **直後に削除する**。

```text
specs/source/<feature-slug>/spec.md
```

L2/L3 に PRD を丸ごと置かない。取り込み後に Source リンクや由来トレースは残さない。詳細は `specs/README.md` / L1 憲法 §3・§5（Source）/ ADR-0003。
