# Product apps

プロダクト本体のコード置き場。

- 現行: `product/apps/kakeibo` — 個人向け家計簿（デモ版）。L3 `specs/L3/kakeibo/` 紐づけ（成熟度 draft）
- 参考実装は `examples/` を見ること（examples 内部の `apps/` は自己完結レイアウト）
- 通常 feature は L2/L3 の決め事に紐づける（draft 可）。`done` は stable 以上
